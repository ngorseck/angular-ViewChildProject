import { AppendDefaultTextDirective } from './append-default-text.directive';

describe('AppendDefaultTextDirective', () => {
  it('should create an instance', () => {
    //const directive = new AppendDefaultTextDirective();
    //expect(directive).toBeTruthy();
  });
});
